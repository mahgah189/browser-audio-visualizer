# Browser Audio Visualizer

A Chrome extension to visualize browser audio.

## Setup

1. Run `npm install`
2. Load the `public` folder as an unpacked extension in Chrome (chrome://extensions → Load unpacked)

## Development

- `npm run lint` — run ESLint
